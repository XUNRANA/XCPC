#include <bits/stdc++.h>
using namespace std;
void solve()
{
	string a,b;
	cin>>a>>b;
	map<char,char>mp;
	char p=a[a.size()-1];
	for(auto i:a)
	{
		mp[p]=i;
		p=i;
	}
	//char p=b[b.size()-1];
	for(auto i:b)
	{
		if(mp[])
	}
}
int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);cout.tie(0);
	int T; 
	cin>>T;
	while(T--) solve();
}
