#include <bits/stdc++.h>
using namespace std;
#define int long long

void solve()
{
    cout<<"Every day with you is a gift, but today is extra special.";

}

signed main() 
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int T=1;
    // cin>>T;
    while(T--) solve();
    return 0;
}