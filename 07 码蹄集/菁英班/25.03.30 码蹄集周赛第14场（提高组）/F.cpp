#include <bits/stdc++.h>
using namespace std;
#define int long long
void solve() 
{
    cin>>n>>q;
    int cnt=0;
    while(q--)
    {
        cin>>l>>r;
        if((r-l+1)&1) cnt
    }
}

signed main() 
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int T=1;
    // cin>>T;
    while(T--) solve();
}