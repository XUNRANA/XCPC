#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int cnt=11;
    while(cnt--)
    {
        string s;
        getline(cin,s);
        cout<<s.size();
    }

}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    int T=1;
    // cin>>T;
    while(T--) solve();
}