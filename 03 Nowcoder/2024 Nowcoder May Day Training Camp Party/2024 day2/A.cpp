#include <bits/stdc++.h>
using namespace std;
int fx(string s)
{
	int ans=0;
	for(int i=0;i<s.size();i++)
	{
		if(s[i]=='(')
	}
	
}
void solve()
{
	string s;
	cin>>s;
	cout<<fx(s);
}
int main()
{
    ios::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
    int T=1;
    cin>>T;
    while(T--) solve();
}
